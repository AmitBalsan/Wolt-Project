interface Restaurant {
  name: string;
  image: string;
  phoneNumber: string;
  bmNumber: string;
  city: number;
  street: string;
}

interface Dish {
  name: string;
  image: string;
  price: number;
  notes: string;
  resID: string;
}
